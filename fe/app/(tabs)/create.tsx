/**
 * Dummy placeholder required by Expo Router's file-based routing.
 * The actual "+" tab press is intercepted in _layout.tsx and
 * redirects to /create-event — this screen never renders.
 */
export default function CreatePlaceholder() {
    return null
}
